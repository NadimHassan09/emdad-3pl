/**
 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}





















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}





















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}





















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}





















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}





















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}
















 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}











 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}









 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



 * Reservation status enum; matches schema reservation_status.
 * Used in stock_reservation to track reservation lifecycle.
 */
export enum ReservationStatus {
  DRAFT = 'DRAFT',
  CONFIRMED = 'CONFIRMED',
  RELEASED = 'RELEASED',
  CANCELLED = 'CANCELLED',
}



























