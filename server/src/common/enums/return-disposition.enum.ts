/**
 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}





















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}





















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}





















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}





















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}





















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}
















 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}











 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}









 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



 * Return disposition enum; matches schema return_disposition.
 * Used in return_order to categorize how returned goods are handled.
 */
export enum ReturnDisposition {
  RESTOCK = 'RESTOCK',
  DAMAGED = 'DAMAGED',
  QUARANTINE = 'QUARANTINE',
  DISPOSAL = 'DISPOSAL',
}



























