# API base URL (default in code: http://127.0.0.1:3000)
#
# Local dev: use .env.development (see repo) or set:
# VITE_API_URL=http://127.0.0.1:3000
#
# Production / hosted API:
# VITE_API_URL=https://emdad-3pl.onrender.com
#
# Restart `npm run dev` after changing env files.
