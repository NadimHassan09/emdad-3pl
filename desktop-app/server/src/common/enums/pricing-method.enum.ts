/** VAS pricing method; matches schema pricing_method. */
export enum PricingMethod {
  FIXED = 'FIXED',
  PER_UNIT = 'PER_UNIT',
  PER_VOLUME = 'PER_VOLUME',
  PER_WEIGHT = 'PER_WEIGHT',
  CUSTOM = 'CUSTOM',
}
