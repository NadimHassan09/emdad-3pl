/** UOM dimension for conversion families; matches schema uom_dimension. */
export enum UomDimension {
  COUNT = 'COUNT',
  LENGTH = 'LENGTH',
  WEIGHT = 'WEIGHT',
  VOLUME = 'VOLUME',
  TEMPERATURE = 'TEMPERATURE',
}
