/** Approval reference type; matches schema approval_reference_type. */
export enum ApprovalReferenceType {
  ADJUSTMENT = 'ADJUSTMENT',
  RETURN = 'RETURN',
  INVOICE = 'INVOICE',
  ORDER = 'ORDER',
}
